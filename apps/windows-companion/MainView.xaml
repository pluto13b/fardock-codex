<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
      Background="#1C1C1E" UseLayoutRounding="True" SnapsToDevicePixels="True"
      TextElement.FontFamily="Segoe UI, Microsoft YaHei UI" TextElement.FontSize="13"
      TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
  <Grid.Resources>
    <!-- ICON_GEOMETRIES -->
    <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#F5F5F7"/></Style>
    <Style TargetType="Button">
      <Setter Property="Background" Value="#0A84FF"/><Setter Property="Foreground" Value="White"/><Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="Padding" Value="15,10"/><Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
        <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="11" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
        <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Opacity" Value=".85"/></Trigger><Trigger Property="IsEnabled" Value="False"><Setter TargetName="Surface" Property="Opacity" Value=".4"/></Trigger><Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Surface" Property="BorderBrush" Value="#A4CEFF"/></Trigger></ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="WindowButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Setter Property="Background" Value="Transparent"/><Setter Property="Width" Value="39"/><Setter Property="Padding" Value="11,9"/></Style>
  </Grid.Resources>
  <Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Grid><TextBlock Text="CODEX PLUS" Foreground="#8E8E93" FontSize="10" VerticalAlignment="Center" Margin="22,0,0,0"/>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True">
      <Button x:Name="MinimizeButton" Style="{StaticResource WindowButton}" ToolTip="最小化" AutomationProperties.Name="最小化"><Viewbox Width="13" Height="13"><Canvas Width="24" Height="24"><Path Data="{StaticResource icon.minus}" Stroke="#A1A1A6" StrokeThickness="1.6"/></Canvas></Viewbox></Button>
      <Button x:Name="CloseButton" Style="{StaticResource WindowButton}" ToolTip="隐藏到托盘" AutomationProperties.Name="隐藏到托盘"><Viewbox Width="13" Height="13"><Canvas Width="24" Height="24"><Path Data="{StaticResource icon.x}" Stroke="#A1A1A6" StrokeThickness="1.6"/></Canvas></Viewbox></Button>
    </StackPanel>
  </Grid>
  <Grid Grid.Row="1" Margin="24,15,24,20">
    <Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="17"/><RowDefinition Height="*"/><RowDefinition Height="29"/></Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal"><Border Width="38" Height="38" Background="#0A84FF" CornerRadius="11" VerticalAlignment="Center"><Viewbox Width="22" Height="22"><Canvas Width="24" Height="24"><Path Data="{StaticResource icon.link}" Stroke="White" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/></Canvas></Viewbox></Border><StackPanel Margin="13,0,0,0" VerticalAlignment="Center"><TextBlock Text="Codex 本地服务" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="Windows 服务端" Foreground="#A1A1A6" FontSize="11" Margin="0,5,0,0"/></StackPanel></StackPanel>
    <Border Grid.Row="2" Background="#2C2C2E" BorderBrush="#3A3A3E" BorderThickness="1" CornerRadius="22" Padding="22,18,22,18">
      <Grid><Grid.RowDefinitions><RowDefinition Height="29"/><RowDefinition Height="39"/><RowDefinition Height="58"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"><Ellipse x:Name="StatusDot" Width="6" Height="6" Fill="#8E8E93" Margin="0,0,9,0" VerticalAlignment="Center"/><TextBlock x:Name="StatusTitle" Text="等待启动" FontSize="21" FontWeight="Medium" VerticalAlignment="Center"/></StackPanel>
        <TextBlock x:Name="StatusDescription" Grid.Row="1" Text="启动后，可在安卓浏览器中使用 Codex。" Foreground="#AEAEB2" FontSize="12" Margin="0,7,0,0" TextWrapping="Wrap" LineHeight="18"/>
        <Grid Grid.Row="2" Margin="0,5,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
          <TextBlock Text="本机 Codex" Foreground="#A1A1A6" FontSize="12"/><TextBlock x:Name="EngineValue" Grid.Column="1" Text="未启动" HorizontalAlignment="Right" FontSize="12"/>
          <TextBlock Grid.Row="1" Text="远程通道" Foreground="#A1A1A6" FontSize="12"/><TextBlock x:Name="GatewayValue" Grid.Row="1" Grid.Column="1" Text="未连接" HorizontalAlignment="Right" FontSize="12"/>
        </Grid>
        <Border x:Name="PairingPanel" Grid.Row="3" Visibility="Collapsed" Background="#222224" CornerRadius="16" Padding="16,12" Margin="0,8,0,16">
          <StackPanel>
            <TextBlock Text="在手机网页中输入" Foreground="#AEAEB2" FontSize="11" HorizontalAlignment="Center"/>
            <TextBlock x:Name="PairingCode" FontFamily="Consolas" FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,7,0,4"/>
            <TextBlock x:Name="PairingExpiry" Foreground="#A1A1A6" FontSize="11" HorizontalAlignment="Center"/>
            <TextBlock Text="你的 Gateway · 先登录账户" Foreground="#A1A1A6" FontSize="11" HorizontalAlignment="Center" Margin="0,8,0,0"/>
          </StackPanel>
        </Border>
        <Grid Grid.Row="4" VerticalAlignment="Bottom"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
          <Button x:Name="ServiceButton" Content="启动服务" Height="39" AutomationProperties.Name="启动服务" FontWeight="SemiBold"/>
          <Button x:Name="PairingButton" Grid.Column="2" Content="连接手机" Height="39" AutomationProperties.Name="连接手机" FontWeight="SemiBold" IsEnabled="False"/>
        </Grid>
      </Grid>
    </Border>
    <TextBlock Grid.Row="3" Text="任务与对话在安卓浏览器操作，关闭窗口保留托盘。" Foreground="#8E8E93" FontSize="10" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
  </Grid>
</Grid>
